export default function DashboardPage() {
  return (
    <main className="mx-auto flex min-h-screen max-w-2xl flex-col items-center justify-center gap-2 px-6 text-center">
      <h1 className="text-2xl font-semibold">Dashboard — coming soon</h1>
      <p className="text-muted-foreground">
        Supabase auth and the diagnostic view will be wired up next.
      </p>
    </main>
  );
}
