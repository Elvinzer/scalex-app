// Public spec-compatible alias. The OAuth implementation remains centralized
// in the legacy path so existing Meta redirect configuration keeps working.
export { GET } from "@/app/api/meta-ads/connect/route";
