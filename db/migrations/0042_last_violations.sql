-- The Support module was already applied as 0037_hot_landau before the
-- migration history was rebased. This marker keeps new deployments aligned
-- without attempting to recreate the existing Support types and tables.
select 1;
